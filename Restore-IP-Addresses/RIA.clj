(def testStr1 "25525511135")
(def testStr1 "0000")
(def testStr1 "010010")
(def testStr1 "101023")

(defn restore-Ip-Addresses [ipstr]
  (let [ippool (atom [])]))

(defn find-next [ipstr ipdonelist]
  "give total ip string and ip done list, return next possible ip"
  )
